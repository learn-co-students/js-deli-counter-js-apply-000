var katzDeliLine = [];

katzDeliLine.forEach(function takeANumber(katzDeliLine, index) {
  var newPosition = index + 1;
  return `Welcome, ${[0]}. You are number ${newPosition} in line.`
  
});

function nowServing(katzDeliLine) {
  if " " {
    return `Currently serving ${[0]}`
  } else {
    return "There is nobody waiting to be served";
  }
  
}

function currentLine(katzDeliLine) {
  var katzDeliLine = ["Ada", "Grace"];
  if [" "] {
  return `The line is currently: 1. ${[0]} 2. ${[1]}`;
  } else {
    return "The line is currently empty.";
  }
}
